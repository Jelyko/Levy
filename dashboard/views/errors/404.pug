doctype
html(lang="en")
    head
        include ../includes/header.pug

        link(rel='stylesheet', href='/css/errors/404.css')
    body
        .jumbotron.text-center.bg-transparent 
            h1.display-3 404
            p.lead Something seems to have gone wrong.
            a.btn.btn-primary(href='/') Return Home